'use strict';

angular.module('edup.subjects')

    .directive('subjects', function () {
        return {
            restrict: 'E',
            templateUrl: 'subjects',
            link: function (scope) {
            }
        };
    }
);